{
  "name": "@fambase/database",
  "version": "1.0.0",
  "description": "Database schema and migrations for FamBase",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@fambase/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}

